import { dingService } from "../service/dingService";

const dingController = {
  /**
   *
   * @param {import('express').Request} req 请求头对象
   * @param {import('express').Response} res 响应头对象
   */
  async jsSdAuthorized(req, res) {},
};

export default dingController;
